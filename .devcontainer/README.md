# Devcontainer for microsoft/graphrag

Copy the `devcontainer.json` and `Dockerfile` to the `.devcontainer` folder in the `microsoft/graphrag` repo to enable exploration of the codebase using a devcontainer.