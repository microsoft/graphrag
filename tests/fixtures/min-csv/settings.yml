llm:
    api_key: ${GRAPHRAG_API_KEY}

embeddings: 
    llm:
        api_key: ${GRAPHRAG_API_KEY}

input:
    type: csv

chunks:
    overlap: ${GRAPHRAG_CHUNK_OVERLAP}
    size: ${GRAPHRAG_CHUNK_SIZE}