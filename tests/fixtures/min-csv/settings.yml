input:
  file_type: csv

embeddings:
  vector_store:
    type: "lancedb"
    db_uri: "./tests/fixtures/min-csv/lancedb"
    container_name: "lancedb_ci"
    overwrite: True

storage:
  type: file
  base_dir: "output"

reporting:
  type: file
  base_dir: "output"

snapshots:
  embeddings: True